<!-- Language detection toast - delayed execution for SEO -->
<!-- data-crawler-ignore: tells search engines this is user-only content -->
<!-- style="display: none" ensures element is hidden before CSS loads -->
<div id="lang-toast" data-crawler-ignore style="display: none" class="fixed bottom-4 left-4 right-4 z-[100] md:left-1/2 md:right-auto md:-translate-x-1/2 md:w-full md:max-w-md max-w-[calc(100vw-2rem)] hidden" aria-labelledby="lang-toast-title" aria-describedby="lang-toast-message" role="dialog">
  <div class="glass p-3 rounded-2xl shadow-2xl border border-[var(--hydrogen-pink)]/30 animate-reveal-up backdrop-blur-xl bg-[#030014]/90">
    <div class="flex flex-col gap-3">

      <!-- Top: Icon + Text -->
      <div class="flex items-center gap-3">
        <span class="text-xl shrink-0" aria-hidden="true">🌐</span>
        <div class="flex-1 min-w-0">
          <h2 class="font-bold text-white text-sm leading-tight mb-0.5" id="lang-toast-title">Change Language?</h2>
          <p class="text-[var(--text-secondary)] text-[11px] leading-tight break-words" id="lang-toast-message">Switch to your local language</p>
        </div>
      </div>

      <!-- Bottom: Buttons (Full Width, 44px min-height for touch targets) -->
      <div class="grid grid-cols-2 gap-2 w-full">
        <button id="lang-toast-close" class="min-h-[44px] py-3 px-3 rounded-xl bg-white/5 hover:bg-white/10 text-[var(--text-secondary)] text-sm font-medium transition-colors border border-white/10 text-center">
          Dismiss
        </button>
        <button id="lang-toast-confirm" class="min-h-[44px] py-3 px-3 bg-white text-pink-600 text-sm font-bold rounded-xl transition-colors shadow-lg shadow-pink-500/20 text-center hover:bg-white/90">
          Switch
        </button>
      </div>

    </div>
  </div>
</div>

<style>
  @keyframes reveal-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .animate-reveal-up {
    animation: reveal-up 0.4s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }
</style>

<script is:inline>
  // Delay language detection to avoid blocking SEO crawlers
  // Run after page fully loads (including images) to ensure user sees content first
  window.addEventListener('load', function() {
    // Additional delay to ensure page is fully rendered and visible
    setTimeout(function() {
      // Only run on root path (English homepage)
      if (window.location.pathname !== '/' && window.location.pathname !== '/index.html') return;

      // 1. Check if user dismissed it before
      if (localStorage.getItem('lang_toast_dismissed')) return;

      // 2. Check browser language
      var userLang = navigator.language || navigator.userLanguage;
      if (!userLang) return;
      userLang = userLang.toLowerCase();

      // Language mapping & UI Text
      var langConfig = {
      'zh': { url: '/zh/', name: '切换 (Switch)', title: '建议切换语言', msg: '检测到您的浏览器为中文，是否切换？' },
      'zh-cn': { url: '/zh/', name: '切换 (Switch)', title: '建议切换语言', msg: '检测到您的浏览器为中文，是否切换？' },
      'zh-sg': { url: '/zh/', name: '切换 (Switch)', title: '建议切换语言', msg: '检测到您的浏览器为中文，是否切换？' },
      'zh-tw': { url: '/zh-tw/', name: '切換 (Switch)', title: '建議切換語言', msg: '檢測到您的瀏覽器為中文，是否切換？' },
      'zh-hk': { url: '/zh-tw/', name: '切換 (Switch)', title: '建議切換語言', msg: '檢測到您的瀏覽器為中文，是否切換？' },
      'zh-mo': { url: '/zh-tw/', name: '切換 (Switch)', title: '建議切換語言', msg: '檢測到您的瀏覽器為中文，是否切換？' },
      'de': { url: '/de/', name: 'Wechseln', title: 'Sprache ändern?', msg: 'Möchten Sie zu Deutsch wechseln?' },
      'ja': { url: '/ja/', name: '切り替え', title: '言語設定', msg: '日本語サイトに切り替えますか？' },
      'ko': { url: '/ko/', name: '전환', title: '언어 변경', msg: '한국어 사이트로 이동하시겠습니까?' },
      'fr': { url: '/fr/', name: 'Changer', title: 'Langue détectée', msg: 'Voulez-vous passer en français ?' },
      'es': { url: '/es/', name: 'Cambiar', title: 'Idioma detectado', msg: '¿Cambiar a español?' },
      'it': { url: '/it/', name: 'Cambia', title: 'Lingua rilevata', msg: 'Passare all\'italiano?' },
      'ru': { url: '/ru/', name: 'Перейти', title: 'Сменить язык', msg: 'Переключиться на русский?' },
      'nl': { url: '/nl/', name: 'Wissel', title: 'Taal wijzigen', msg: 'Wissel naar Nederlands?' },
      'pl': { url: '/pl/', name: 'Zmień', title: 'Zmień język', msg: 'Przełącz na polski?' }
    };

    // Try match
    var target = langConfig[userLang];
    if (!target) {
      var mainLang = userLang.split('-')[0];
      target = langConfig[mainLang];
    }

    // If target found and it's NOT English (since we are on English page)
    if (target && !userLang.startsWith('en')) {
      var toast = document.getElementById('lang-toast');
      var titleEl = document.getElementById('lang-toast-title');
      var msgEl = document.getElementById('lang-toast-message');
      var confirmBtn = document.getElementById('lang-toast-confirm');
      var closeBtn = document.getElementById('lang-toast-close');

      if (toast && titleEl && msgEl && confirmBtn && closeBtn) {
        // Update UI
        titleEl.textContent = target.title;
        msgEl.textContent = target.msg;
        confirmBtn.textContent = target.name;

        // Show toast with a small animation delay for better UX
        requestAnimationFrame(function() {
          toast.style.display = 'block';
          // Also remove hidden class for compatibility
          toast.classList.remove('hidden');
        });

        // Handlers
        confirmBtn.onclick = function() {
          localStorage.setItem('preferred_path', target.url);
          window.location.href = target.url;
        };

        closeBtn.onclick = function() {
          toast.style.display = 'none';
          toast.classList.add('hidden');
          localStorage.setItem('lang_toast_dismissed', 'true');
        };
      }
    }
    }, 2000); // 2 second delay after page fully loads (images, css, etc.)
  });
</script>
