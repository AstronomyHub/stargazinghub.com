---
import Layout from '../../layouts/Layout.astro';
import Hero from '../../components/Hero.astro';
import Features from '../../components/Features.astro';
import Testimonials from '../../components/Testimonials.astro';
import DownloadSection from '../../components/DownloadSection.astro';
import CompassPromo from '../../components/CompassPromo.astro';
import Footer from '../../components/Footer.astro';
import { translations } from '../../i18n/ui';

const lang = 'es';
const t = translations[lang];
---

<Layout title={t.meta?.title || 'Stargazing Hub: mapa del cielo, contaminación lumínica y auroras'} lang={lang} description={t.meta?.description}>
  <main>
    <Hero t={t} lang={lang} />
    <Features t={t} />
    <Testimonials t={t} />
    <DownloadSection t={t} />
    <CompassPromo lang={lang} />
  </main>
  <Footer t={t} lang={lang} />
</Layout>
